# API key for /v1/score (developer API). Not needed for the main chat page.
API_KEY=dev-key-change-in-production

# AI teacher: add your OpenAI key to get real AI answers, difficulty, relevance, and explanations.
# Without it, the site still works using built-in scoring + SymPy for equations.
OPENAI_API_KEY=
