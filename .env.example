# API key for /v1/* endpoints. Generate one for local dev, e.g. openssl rand -hex 16
API_KEY=your-api-key-here

# Optional: set for Bearer token auth (same value as API_KEY or separate)
# BEARER_TOKEN=
